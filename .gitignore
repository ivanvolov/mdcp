# Carcass boundary artifact — never push to this repo's remote
github.pointer.md

node_modules/
dist/
.env
.env.*
!.env.example
*.log

# Per-run strategy state (regenerated by the benchmark arms)
.state/
.state-*/

# Default BENCH_LOG of the arm scripts — scratch. Curated evidence lives in
# the numbered app/bench/logs/s*/ directories and IS committed.
app/bench/logs/*.jsonl
